@startuml

|User|
start;
: Відкриває сторінку профілю;
: Змінює необхідні дані (email, пароль, ім'я або прізвище);
: Натискає кнопку "Зберегти";

|Система|
: Перевіряє нові дані;
note right #fcf938
<b> Неправильний формат облікових даних
<b> Проблеми із сервером
<b> Дані повторюються - тобто не змінені
end note
: Зберігає зміни;

|User|
: Отримує підтвердження про успішне оновлення даних;

stop;

@enduml
